#include "Draw.h"

void Draw::grid(sf::RenderWindow* window, Grid grid) {
	std::vector<std::vector<Plant*>> gridVec = grid.getGrid();
	
	sf::RectangleShape line;

	for (int i)
	line.setPosition(gridVec)
}

